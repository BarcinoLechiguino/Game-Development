#include <stdio.h>
#include <stdlib.h>
#include <iostream>
#include "Ein Klass.h"

int main()
{
	Vec3 <float> Vector(1, 2, 3);
	Vec3 <float> VectorII(1, 2, 3);
	Vec3 <float> VectorIII(3, 2, 1);
	Vec3 <float> VectorIV(2, 4, 6);
	Vec3 <float> VectorV(0, 0, 0);
	Vec3 <float> VectorVI(1, 0, 0);
	Vec3 <float> VectorVII(0, 1, 0);
	Vec3 <float> VectorVIII(0, 0, 1);
	Vec3 <float> VectorIX(1, 1, 0);
	Vec3 <float> VectorX(1, 0, 1);
	Vec3 <float> VectorXI(0, 1, 1);
	Vec3 <float> emptyVector();

	/*//Get Vector function examples
	Vector.getVector();
	VectorII.getVector();
	VectorIII.getVector();
	VectorIV.getVector();
	emptyVector.getVector();

	//Get Functions
	Vector.getx();
	Vector.gety();
	Vector.getz();

	//Operator use example
	Vector.operator+(VectorII);
	Vector.getVector();

	Vector.operator-(VectorII);
	Vector.getVector();

	Vector.operator+=(VectorII);
	Vector.getVector();

	Vector.operator-=(VectorII);
	Vector.getVector();

	Vector.operator=(VectorIII);
	Vector.getVector();

	Vector.operator==(VectorII);
	Vector.operator==(VectorIII);

	//isZero function example
	Vector.isZero();
	VectorIV.isZero();
	VectorV.isZero();
	VectorVI.isZero();
	VectorVII.isZero();
	VectorVIII.isZero();
	VectorIX.isZero();
	VectorX.isZero();
	VectorXI.isZero();

	//Set to Zero function example
	Vector.setToZero();
	Vector.getVector();

	//Normalize function example
	Vector.normalize();
	Vector.getVector();

	//DistanceTo function example
	Vector.distanceTo(VectorIV);*/

	system("pause");
	return 0;
}